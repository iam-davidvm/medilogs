

<!DOCTYPE html>
<html lang="nl">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://fonts.googleapis.com/css2?family=Nunito+Sans:wght@400;700;800&display=swap" rel="stylesheet" crossorigin> 
    <link rel="stylesheet" href="/css/style.css">
    <% if(typeof results !== 'undefined') { %>
<!-- WEG
        <script src="https://cdn.jsdelivr.net/chartist.js/latest/chartist.min.js" crossorigin></script>
        <script src="https://cdnjs.cloudflare.com/ajax/libs/chartist-plugin-legend/0.6.2/chartist-plugin-legend.min.js" crossorigin></script>
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/chartist.js/latest/chartist.min.css" crossorigin> -->
        <link rel="stylesheet" href="/css/chartist.css">
        <!-- <link rel="stylesheet" href="/css/overflow-test.css"> -->
            
    <% } %>

    <%- include('../partials/title') %>
</head>
<body>
    <div class="modal invisible " id="cookies-modal">
        <section class="cookies-consent box hard-shadow">
            <h2>Wij gebruiken cookies</h2>
            <p>Wij maken gebruik van essentiële cookies om deze website te laten werken. Dit zijn strikt noodzakelijke cookies die niet van derde partijen komen.</p>
            <a href="" class="cta cookies-accept">Ik heb het begrepen</a>
        </section>
    </div>

    <nav class="admin-bar">
        <ul>
            <li><a href="/admin/users" class="cta">Users</a></li>
            <li><a href="/admin/maintenance" class="cta">Maintenance</a></li>
            <li><a href="/admin/maintenance" class="cta">Maintenance</a></li>
            <li><a href="/admin/maintenance" class="cta">Maintenance</a></li>
        </ul>
    </nav>

    <section class="maintenance-bar">
        <div class="container">
            <p id="maintenance-msg" aria-live="polite"></p>
        </div>
    </section>
    <%- include('../partials/warning') %>
    <header>
        <div class="container">
            <nav class="navbar">
                <div class="navbar-brand">
                    <% if(!currentUser) { %>
                        <p><a href="/">medilog</a></p>
                        <% } else { %>
                            <p><a href="/<%= currentUser.patienten[0]._id %>/dashboard/">medilog</a></p>
                    <% } %>
                </div>
                <div class="navbar-mobile" aria-expanded="false">
                    <input type="checkbox" name="nav-mobile" id="mobile-menu">
                    <label for="mobile-menu" id="nav-icon"></label>
                </div>
                <div class="navbar-user" aria-live="polite">
                    <% if(!currentUser) { %>
                        <a href="/registreren" class="btn btn-sec">Registreren</a>
                        <a href="/aanmelden" class="btn btn-pri">Aanmelden</a>
                    <% } else { %>
                        <a href="/afmelden" class="btn btn-sec">Afmelden</a>
                    <% } %>
                </div>
            </nav>
        </div>
    </header>
    <main>
        <div class="container">
            <%- include('../partials/flash') %>
          
            <%- body %>
        </div>
    </main>

    <script src="/js/maintenance.js"></script>
    <script src="/js/cookies-consent.js"></script>
    <script>
        const mobileMenu = document.getElementById('mobile-menu');
        mobileMenu.checked = false;
        if (mobileMenu) {
            mobileMenu.addEventListener('change', function() {
                const navbarUser = document.getElementsByClassName('navbar-user')[0];
                const navbarMobile = document.getElementsByClassName('navbar-mobile')[0];
                if (this.checked) {
                    console.log("Checkbox is checked..");
                    navbarUser.classList.add('reveal-mobile');
                    navbarMobile.setAttribute('aria-expanded', true);
                } else {
                    console.log("Checkbox is not checked..");
                    navbarUser.classList.remove('reveal-mobile');
                    navbarMobile.setAttribute('aria-expanded', false);
                }
            })
        }
    </script>
</body>
</html>